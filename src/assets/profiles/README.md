# Fotos de pasaporte

Deja aquí `irene.webp` y `vicente.webp` (o .png/.jpg) y aparecerán solas en el
Pugporte. El nombre del fichero es lo que las empareja con cada persona, así
que tiene que ser exactamente `irene` y `vicente`, en minúsculas.
